# 4건 동시 리서치 재측정 — 2026-09-10

## 결과

21:43:46 KST에 AAPL·MSFT·NVDA·AMZN을 함께 접수했다. 서버 Pro 로그인, 모든 모델 Luna, API 비활성화. 이전 최종 수정 실행 파일 SHA256 `46de55a444258a342360764a85a9452f71fe80a2c79e2f987cce50f06dfcf1c4`로 시작해 무개입 측정이 끝날 때까지 코드 교체·워커 재시작·수동 재개를 하지 않았다.

| 종목 | 접수→발행/실패 | 무개입 결과 |
|---|---:|---|
| NVDA | 23분 14초 | 제한사항 명시, 발행 완료 |
| AMZN | 23분 47초 | 제한사항 명시, 발행 완료 |
| AAPL | 26분 38초 | 제한사항 명시, 발행 완료 |
| MSFT | 30분 12초 | 분석 29개 완료 후 발행 검사 오탐, 자동 복구 소진 |

MSFT 분석 자체는 약 27분 35초에 끝났다. 실패를 성공 시간으로 계산하지 않았다. 무개입 성공률은 이 표본에서 3/4이며 통계적 안정성 보장은 아니다. 수정 후 동일 실행의 발행만 재개해 보고서를 복구했다. 이 복구는 위 무개입 결과와 분리한다.

## 관측과 개선점

- 31회 자원 표본: 활성 리서치 최대 4건, 활성 작업 최대 12개(각 리서치 3개), 워커 CPU 최대 145.48%(2코어 합산 200% 기준), 메모리 최대 707.5MiB. 운영 웹 HTTP 상태는 모두 200. 이 검증은 운영과 같은 서버의 별도 컨테이너·데이터 디렉터리를 사용했다.
- 실행 시작→수집 완료: NVDA 1분 56초, AMZN 2분 1초, AAPL 6분 57초, MSFT 9분 12초. 접수→실행 시작 약 13초는 별도다.
- 초기 뉴스 AI 선별: AAPL 입력 49,341/출력 10,859토큰, MSFT 입력 39,006/출력 7,864토큰. 뒤에 상세 분류 호출도 이어진다. 후보 수·출력 필드·길이를 줄이고 수집 단계별 시간 계측을 추가하는 것이 개선 후보다. 전체 수집 시간의 정확한 귀속은 아직 분리 계측하지 않았다.
- 후반 의미 검증·최종 종합이 순차로 이어진다. 단일 호출 약 101~168초가 기록됐다. AMZN은 의미 검증 재작성이 추가됐다. 동시 슬롯만 늘려서는 이 순차 시간을 없애지 못한다.
- 모델 출력 자동 재작성: AMZN 2회, NVDA 2회. MSFT 최종 종합에서 `version_probe` 일시 오류 1회가 자동 복구됐다.
- Node 프로세스가 모델 작업 1~2개만 남았을 때도 CPU 약 한 코어를 사용한 관측이 있다. 소스상 250ms마다 reconcile하면서 이미 완료된 구조 검증 입력을 재구성하고, round authority 생성 때 32개 마이그레이션 파일 해시·전체 foreign_key_check를 반복하는 경로가 있다. CPU 점유율의 정확한 원인 비중은 프로파일링하지 않았으므로 후보로 분류한다. 전체 reconcile 주기 분리, 완료된 단계의 증거 재로딩 생략, DB authority 재사용을 검토할 수 있다. 이번 실행에서는 이 최적화를 적용하지 않았다.

## 새로 발견해 수정한 발행 오류

MSFT 문장은 “rather than a precise target price”라며 정밀 목표가 제시를 피하고 있었다. 최종 V3 본문 투영이 이미 정규화된 문장을 원문으로 덮어써 `target price` 금지 정규식에 걸렸다. 실제 추정 목표가가 발견된 사례가 아니다.

`src/research/domain/report.ts`의 canonical 본문 덮어쓰기에도 기존 `normalizeReportNarrativeText`를 적용했다. 금지 스키마·근거·출처·lineage 검사를 해제하지 않았다. 실제 MSFT 문단으로 실패를 먼저 재현하고 관련 테스트 16개 통과, 워커 타입 검사·빌드 통과. 다른 기존 테스트는 이번에 광범위하게 반복하지 않았다.

수정 실행 파일 SHA256: `2ae9a56089b9218654f4a453a6380b15db85f2ad4c7068e61a87bea81b23e23e`. 동일 MSFT 실행에서 발행 단계만 재개했고 본 모델 기록은 전후 모두 112개로 추가 호출이 없다. 4개 보고서 CAS 내용·크기·해시와 발행 상태를 확인했다.

## 보존 자료 및 정리

- `research-concurrency-rerun-2026-09-10.baseline.json`: 개입 전 실패를 포함한 타임라인·호출·자원 기록.
- `research-concurrency-rerun-2026-09-10.recovery.json`: 수정 후 발행·CAS 검증·정리 기록.
- `.artifacts/reliability/rerun-observations.jsonl`: 분 단위 관측 원본.
- `.artifacts/reliability/canonical-disclaimer-{red,green,typecheck}.log`: 회귀 검증.
- 서버 검증 데이터 `/home/ec2-user/stocksembly-reliability-rerun-20260910/data`는 근거로 보존. 검증 컨테이너와 임시 환경 파일은 제거.
- 운영 배포·Git 커밋/푸시·API 모델 실호출은 하지 않았다. 작업 브랜치 `codex/research-capacity-routing`의 기존 변경과 원래 작업 디렉터리의 다른 변경을 보존했다.
