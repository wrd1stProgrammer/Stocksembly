# NVDA 단독 리서치 시간 측정 — 2026-09-11

## 결과

00:01:23.655 KST 접수 → 00:21:18.919 KST 발행, **19분 55초**. 29개 작업 모두 성공, 제한사항 명시 보고서 발행 및 CAS 해시 확인. 실행 중 수정·재시작·수동 복구 없음. 전체 21회 관측에서 운영 리서치 실행/대기/취소중 0건, 운영 웹 HTTP 200으로 단독 서버 조건 유지.

| 구간 | 이번 단독 NVDA | 앞선 4건 동시 NVDA |
|---|---:|---:|
| 접수→발행 | 19분 55초 | 23분 14초 |
| 접수→실행 | 17초 | 13초 |
| 실행→수집 완료 | 5분 49초 | 1분 56초 |
| 수집 완료→발행 | 13분 49초 | 21분 5초 |

총시간은 단독이 3분 19초 짧았다. 수집 이후는 7분 16초 짧았지만, 같은 저장 입력을 재생한 통제 실험은 아니다. 이번에는 뉴스 선별/상세 AI 2회가 실행됐고 이전 NVDA에는 해당 보조 호출 기록이 없었다. 모델 자동 재작성도 이번 3회(회사 메모1, 회사 종합2), 이전2회로 달랐다. 따라서 정확한 동시성 지연율이나 기존10분 대비 회귀율을 산출할 수는 없다.

## 발견한 중요한 설정 차이

이번 동시 처리 구현에서 추가한 `RESEARCH_EXECUTION_LIMITS.jobsPerRun=3`가 단독 실행에도 적용된다. 실제 최대 병렬 작업3개를 관측했다. 이전 기본 설정은 전체 Codex 작업6개이며 이 per-run3 제한이 없었다. 고정3 상한은 단독 실행 때 남는 슬롯을 사용하지 못하게 하므로 단독 성능을 낮출 수 있는 구현상의 차이다. 이번에는 측정 조건을 유지하기 위해 변경하지 않았다.

개선 방향은 단독에는 기존 수준의 여유 슬롯을 허용하고, 경쟁 리서치가 들어오면 공정하게 배분하는 방식이다. 정확한 단축 폭은 변경 후 확인해야 한다. 초기 뉴스 선별의 큰 입출력(48,523 입력/11,406 출력 토큰)과 상세 분류, 모델 재작성, 마지막 순차 검증·종합도 별도 시간 요인이다. '4건 동시 때문에만 느리다'고 결론 내릴 수 없다.

## 실행 조건과 정리

- 서버Pro 로그인, 모델 전부 Luna, API 비활성화. 모델 API 호출 없음.
- 최종 수정 워커 SHA256 `2ae9a56089b9218654f4a453a6380b15db85f2ad4c7068e61a87bea81b23e23e`. 앞선 동시 측정 이후 변경은 canonical 본문 발행 정규화 수정이며 scheduler는 동일.
- 최대 관측 워커 CPU106.26%(2코어 합산200% 기준), 메모리560.5MiB. 일부 구간에서 CPU한코어에 근접했지만 지속 시스템 전체 포화로 해석하지 않는다.
- 검증 컨테이너 `stocksembly-single-20260911`와 임시 환경파일 제거. 데이터 `/home/ec2-user/stocksembly-single-20260911/data` 보존.
- 실행 ID `0285e98d-b2e2-416f-9c68-e5902b1c2070`.
- `research-single-2026-09-11.evidence.json`에 시간·호출·모델·CAS·정리 결과, `.artifacts/reliability/single-observations.jsonl`에 관측 원본 보존.
- 이번 턴은 단독 실행 측정만 수행. 앱 코드 변경, 운영 배포, Git 커밋/푸시 없음.
