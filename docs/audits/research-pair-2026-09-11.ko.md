# AMD·NFLX 2건 동시 리서치 — 2026-09-11

리서치당 최대10개, 전역 최대40개 설정과 직전 실행 파일을 그대로 사용했다. 실제 서버 Pro CLI·Luna, 모델 API 비활성화. 두 건 모두 중간 수정·재시작·수동 복구 없이 29개 작업과 제한사항 명시 보고서 발행을 완료했다.

| 종목 | 접수→발행 | 접수→수집 완료 | 수집 후→발행 |
|---|---:|---:|---:|
| NFLX | 14분59초 | 4분9초 | 10분49초 |
| AMD | 16분24초 | 5분54초 | 10분31초 |

구간별 반올림으로 총합에1초 차이가 있을 수 있다. 직전4건은21분44초~26분10초였다. 이번은 더 짧았지만 종목·자료·출력과 재작성 수가 달라 동시 사용자 수만의 효과로 단정할 수 없다.

## 부하와 오류

- 관측61회, 약16초 간격. 다른 운영 리서치 실행/대기/취소중0건 유지.
- 실행 상태 작업 최대14개, 각 리서치는10개씩 관측. Codex 프로세스 동시 최대8개 관측. 순간 피크는 표본 사이에서 누락될 수 있으며, 작업 상태 수와 실제 모델 프로세스 수는 다르다.
- 워커 CPU 최대139.82%(2코어 합산200% 기준), 메모리 최대549.6MiB.
- 운영 웹 localhost HTTP 중앙값0.534초, p95 0.869초, 최대0.891초. 전부200. 외부망/TLS/CDN 또는 여러 웹 사용자의 동시 접속 부하는 별도다.
- AMD 출력 재작성2회. 수치/지표 불일치1건이 보정 대상으로 기록됐다. 실행 준비/버전/바이너리 검사 실패나 최종 발행 실패는 없었다.
- 전체 메인 모델 기록57개, 모두Luna. 보고서2개 CAS 내용·길이·해시와 발행 상태 확인.

## 실행 조건과 정리

실행 파일 SHA256 `d0b2e06e6b0b6501cd52ea82db17c12862960b2101e5d7d261db1aec7cb1cedb`. 신규2사용자에 해당하는 별도 principal로 실제 admission repository에 빈 질문·영문·위원회 리서치를 접수했다. UI/로그인/HTTP 접수 앞단은 재생하지 않았다. 운영과 같은 EC2·Node20 이미지·Pro 인증·실제 데이터 공급자를 사용했고, 별도 컨테이너·SQLite·CAS로 기존 사용자 데이터와 분리했다.

`research-pair-2026-09-11.evidence.json`에 시간·호출·자원·CAS·정리 결과, `.artifacts/reliability/pair-observations.jsonl`에 관측 원본 보존. 검증 컨테이너 `stocksembly-pair-20260911`와 임시 환경 파일 제거. `/home/ec2-user/stocksembly-pair-20260911/data`는 증거로 보존했다.

이번 턴은 요청한2건 실측만 수행했다. 앱 코드 변경·운영 배포·Git 커밋/푸시·PR 생성·모델 API 호출은 하지 않았다.
